version: 2

seeds:
  - name: employees
    description: a manual map of employees to costumers
    columns:
      - name: employee_id
        tests:
          - unique
          - not_null
          
